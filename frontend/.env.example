# Copy to `.env` or `.env.local` in this directory.
# Vite exposes only variables prefixed with VITE_.

# Backend URL for the dev-server proxy (see vite.config.ts).
VITE_API_PROXY=http://127.0.0.1:8000
